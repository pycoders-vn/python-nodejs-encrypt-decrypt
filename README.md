# AES Encrypt in node and decrypt in python

Use cases:

- Encrypt data for client application such as: Angulat, ReactJS
- Decrypte data in python backend.

Install required packages:

```shell
pip3 install pycryptodomex
```